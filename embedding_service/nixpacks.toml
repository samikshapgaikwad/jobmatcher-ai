[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m spacy download en_core_web_sm"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
```

**`embedding_service/requirements.txt`** — make sure it has everything:
```
fastapi
uvicorn
python-multipart
sentence-transformers==3.3.1
transformers==4.47.0
spacy
supabase==2.7.4
python-dotenv
apscheduler
pymupdf
python-docx
websockets==13.1